<Window x:Class="Sidequest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sidequest"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Topmost="True" 
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="Transparent"
        MouseEnter="Window_Expand"
        MouseLeave="Window_Collapse"
        Height="320" Width="320">
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="17.5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FocusVisual1">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background1" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border1" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background1" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border1" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background1" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border1" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background1" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border1" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground1" Color="#FF838383"/>
        <Style x:Key="ButtonStyle1" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual1}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background1}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border1}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="17.5" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background1}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border1}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background1}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border1}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background1}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border1}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground1}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border 
        x:Name="MainBorder"
        BorderBrush="#2a2a2a" 
        BorderThickness="2" 
        CornerRadius="10"
        Background="#121212"
        >

        <Grid
            x:Name="MainGrid">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button 
                x:Name="ActiveQuestsButton"
                Style="{Binding}"
                Click="ShowActive" 
                Margin="10,10,231,257" 
                BorderThickness="3,3,3,0"
                Content="Active" 
                FontFamily="Arial" 
                FontSize="16" 
                FontWeight="Bold" 
                Foreground="#6e6e6e"
                Background="#121212"
                BorderBrush="#121212"

                />

            <Button
                x:Name="FinishedQuestsButton"
                Style="{Binding}"
                Click="ShowFinished" 
                Margin="160,10,81,257" 
                BorderThickness="3,3,3,0" 
                Content="Finished" 
                FontFamily="Arial" 
                FontSize="16" 
                FontWeight="Bold"
                Foreground="#6e6e6e"
                Background="#121212"
                BorderBrush="#121212"
                
                />

            <Button
                x:Name="OverdueQuestsButton"
                Style="{Binding}"
                Click="ShowOverdue" 
                Margin="85,10,156,257" 
                BorderThickness="3,3,3,0" 
                Content="Overdue" 
                FontFamily="Arial" 
                FontSize="16" 
                FontWeight="Bold"
                Foreground="#6e6e6e"
                Background="#121212"
                BorderBrush="#121212"
                
               />

            <ListBox x:Name="ActiveQuests"
                     ItemsSource="{Binding listQuests}"
                     Margin="10,40,10,10" 
                     BorderBrush="#121212" 
                     Background="#121212"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                     Grid.Column="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,12" Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text= "{Binding QuestName}" Foreground="White" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Deadline}" Foreground="#a0a0a0" FontSize="14" Margin="0,3,0,0"/>
                                <TextBlock x:Name="QuestContentsTextBox" Visibility="Collapsed" Text="{Binding QuestContents}" Foreground="#a0a0a0" FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>


                            <StackPanel Grid.Column="1" Orientation="Horizontal">

                                <Button Style="{DynamicResource ButtonStyle1}"
                                        Width="25"
                                        Height="25"
                                        Margin="10,0,0,0"
                                        Background="#6e6e6e"
                                        Cursor="Hand"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Click="RemoveQuest">

                                    <TextBlock Text="×" Foreground="Red" FontWeight="Bold" Margin="0,-2,0,0"/>

                                </Button>

                                <Button Style="{DynamicResource ButtonStyle1}"
                                        Width="25"
                                        Height="25"
                                        Margin="10,0,0,0"
                                        Background="#6e6e6e"
                                        Cursor="Hand"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Click="FinishQuest">    

                                    <TextBlock Text="✓" Foreground="LightGreen" FontWeight="Bold" Margin="0,-2,0,0"/>

                                </Button>

                                <Button Style="{DynamicResource ButtonStyle1}"
                                        Width="50"
                                        Height="25"
                                        Margin="10,0,0,0"
                                        Background="#6e6e6e"
                                        Cursor="Hand"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Click="ToggleEditQuestUI">   

                                    <TextBlock Text="Edit" Foreground="#2a2a2a" FontWeight="Bold" Margin="0,-2,0,0"/>

                                </Button>
                                
                            </StackPanel>
                            
                            

                        </Grid>

                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                <Setter TargetName="QuestContentsTextBox" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>


            <ListBox x:Name="FinishedQuests"
                    ItemsSource="{Binding listFinishedQuests}"
                    Visibility="Collapsed"
                    Margin="10,40,10,10" 
                    BorderBrush="#121212" 
                    Background="#121212"
                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                    Grid.Column="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding QuestName}" Foreground="White" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Deadline}" Foreground="#a0a0a0" FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>

                            <Button Grid.Column="1"
                                    Style="{DynamicResource ButtonStyle1}"
                                    Width="25"
                                    Height="25"
                                    Margin="10,0,0,0"
                                    Background="#6e6e6e"
                                    Cursor="Hand"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Click="RemoveQuest">

                                <TextBlock Text="×" Foreground="Red" FontWeight="Bold" Margin="0,-2,0,0"/>

                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>


            <ListBox x:Name="OverdueQuests"
                    ItemsSource="{Binding listOverdueQuests}"
                    Visibility="Collapsed"
                    Margin="10,40,10,10" 
                    BorderBrush="#121212" 
                    Background="#121212"
                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                    Grid.Column="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding QuestName}" Foreground="White" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Deadline}" Foreground="#a0a0a0" FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>

                            <Button Grid.Column="1"
                                    Style="{DynamicResource ButtonStyle1}"
                                    Width="25"
                                    Height="25"
                                    Margin="10,0,0,0"
                                    Background="#6e6e6e"
                                    Cursor="Hand"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Click="RemoveQuest">

                                <TextBlock Text="×" Foreground="Red" FontWeight="Bold" Margin="0,-2,0,0"/>

                            </Button>

                            <Button Grid.Column="1"
                                    Style="{DynamicResource ButtonStyle1}"
                                    Width="25"
                                    Height="25"
                                    Margin="45,0,0,0"
                                    Background="#6e6e6e"
                                    Cursor="Hand"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Click="FinishOverdueQuest">             

                                <TextBlock Text="✓" Foreground="LightGreen" FontWeight="Bold" Margin="0,-2,0,0"/>

                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel
                x:Name="NewQuestEntry"
                Visibility="Collapsed"
                VerticalAlignment="Bottom">

                <TextBox
                    x:Name="NewQuestEntryTextBox"
                    Width="260"
                    Background="#121212"
                    Foreground="White"
                />

                <TextBox
                    x:Name="NewQuestContentTextBox"
                    Width="260"
                    Height="30"
                    Background="#121212"
                    Foreground="White"/>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,5" HorizontalAlignment="Center">
                    <DatePicker x:Name="NewQuestDeadlineDate" Width="100"/>

                    <TextBox x:Name="NewQuestDeadlineTime" Width="45" Text="23:59" 
                     Margin="5,0,0,0" Background="#121212" Foreground="White" 
                     VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                     BorderBrush="#707070" BorderThickness="1"/>    
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Estimated time:" Foreground ="White"/>

                    <TextBox x:Name="NewQuestTimeEstimate" Width="45" Text="01:00" 
                             Margin="5,0,0,5" Background="#121212" Foreground="White" 
                             VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                             BorderBrush="#707070" BorderThickness="1"/>
                    
                </StackPanel>


                <Button 
                    Content="Save" 
                    Click="SaveNewQuest"
                    Width="50"
                    Background="#6e6e6e"
                    FontWeight="Bold"
                />

            </StackPanel>


            <Button Style="{DynamicResource ButtonStyle1}"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="251,251,0,0"
                Background="#6e6e6e"
                BorderThickness="0"
                Cursor="Hand"
                Height="35"
                Width="35" 
                Click="ToggleNewQuestUI">

                <Path Data="M0,7 H14 M7,0 V14" 
                    Stroke="Black" 
                    StrokeThickness="3" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                    Width="14" 
                    Height="14" />

            </Button>

            <Button Style="{DynamicResource ButtonStyle1}"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="271,10,0,0"
                Background="#2a2a2a"
                BorderThickness="0"
                Cursor="Hand"
                Height="15"
                Width="15" 
                Click="Button_Exit">

                <Path Data="M0,7 H14 M7,0 V14" 
                    Stroke="Black" 
                    StrokeThickness="2" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                    Width="5" 
                    Height="5" />

            </Button>


        </Grid>

    </Border>
</Window>
